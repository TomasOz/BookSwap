<!-- Header Section -->
<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <div class="container">
    <a class="navbar-brand" href="#">Books Board</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto"> <!-- Adjusted for left alignment -->
        <li class="nav-item">
          <%= link_to 'Home', root_path, class: 'nav-link' %>
        </li>
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to 'My Books', books_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'SideKiq Testing', sidekiq_testing_path, class: 'nav-link' %>
          </li>
        <% end %>
        <!-- Add other menu items here -->
      </ul>

      <!-- Right-aligned Log In / Sign Up / Log Out buttons -->
      <ul class="navbar-nav ms-auto"> <!-- Added ms-auto for right alignment -->
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to 'Log Out', logout_path, method: :delete, data: { confirm: 'Are you sure you want to log out?' }, class: 'nav-link btn btn-danger' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log In', login_path, class: 'nav-link btn btn-outline-primary me-2' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Up', signup_path, class: 'nav-link btn btn-primary' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>